# web/

React 前端源码目录。由 Vite 构建，产物输出到项目根的 `public/static/`。

## 常用命令

```bash
npm install        # 安装依赖
npm run dev        # 开发服务器（:5173）
npm run build      # 生产构建（输出到 ../public/static）
npm run lint       # 代码检查
```

> 一般在项目根目录用 `npm run dev` / `npm run build` 统一调度，无需进入此目录。
